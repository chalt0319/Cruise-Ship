<h1>Welcome <%=@passenger.name%>!</h1>

<h3>Your ship is: <%=@passenger.ship.name%></h3>

<h3>Your Excursions:</h3>

<ul>
  <%@excursions.each do |e|%>
  <li><%=e.title%> - <%=e.port.location%> - <%=e.duration%> hour(s) - <%=link_to("Delete", edit_passenger_excursion_path(@passenger, e))%></li>
  <%end%>
</ul>

<%=link_to("Add Excursion", new_passenger_excursion_path(@passenger))%><br>
<%=link_to("Logout", logout_path)%>
